<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      TinyCanvas Adventures - Fire OS tablet target.
      No Google Play Services, no billing, no ads, no analytics.
      INTERNET is required only for Amazon IAP product data / checkout.
      No location, contacts, camera, microphone, or advertising-ID permission.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Legacy external-storage write is limited to API <= 28 (older Fire OS).
         Modern Fire OS uses scoped MediaStore inserts with no permission. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Tablet-only product; declare touchscreen requirement explicitly. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="true" />

    <application
        android:label="TinyCanvas Adventures"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Amazon IAP ResponseReceiver (Appstore SDK v3). Enabled by the
             amazon build flavor; harmless when the SDK is absent in dev. -->
        <receiver
            android:name="com.amazon.device.iap.ResponseReceiver"
            android:exported="true"
            android:permission="com.amazon.inapp.purchasing.Permission.NOTIFY"
            tools:ignore="MissingClass"
            xmlns:tools="http://schemas.android.com/tools">
            <intent-filter>
                <action android:name="com.amazon.inapp.purchasing.NOTIFY" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
