# iOS (stub only - NOT a build target)

TinyCanvas Adventures targets Amazon Fire OS tablets (Android) ONLY.
This folder exists solely so online IDEs / importers that validate the
standard Flutter project layout (e.g. FlutLab) accept the project.
Do not build for iOS.
